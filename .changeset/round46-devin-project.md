---
"attnbox-collectors": patch
---

Devin sessions with a pull request now carry the repo (owner/repo) as their project, so the grouped inbox view organizes cloud sessions by repository instead of one flat "devin" bucket.

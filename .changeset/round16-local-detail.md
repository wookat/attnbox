---
"attnbox-collectors": patch
"attnbox": patch
---

Local waiting items now say what for: Claude Code previews the last assistant message, Codex previews the pending approval (`wants to run: <command>` / `wants to apply a patch`) — same read-only files, no new I/O.

---
"attnbox": patch
---

Default inbox view collapses finished sessions behind a "Show N finished sessions" expander (76% of a real dogfood dataset was finished history drowning the actionable cards). Done tab, search, and grouped view are unchanged.

---
"attnbox-collectors": patch
"attnbox": patch
---

github-pr fallback: bad tokens (401/403) now warn on stderr instead of silently dropping review requests, and `attnbox doctor` live-probes the GitHub token like it does the Devin key.

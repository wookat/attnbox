---
"attnbox": patch
---

Mobile inbox ergonomics: the search box and filter tabs now stay pinned below the header while scrolling long session lists, and titles wrap to two lines on phones instead of truncating to one.

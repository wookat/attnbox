---
"attnbox": patch
---

The Needs-you section gains a "✓ all done" button to ack every visible waiting item at once (respects the active search/filter; same persistence and resurface semantics as single acks).

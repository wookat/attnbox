---
"attnbox": patch
---

Mobile: reply-box placeholder no longer overflows the textarea (shortcut hint moved to a tooltip); grouped-view fallback buckets render as "agent · no project" instead of "(agent)".

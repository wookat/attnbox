---
"attnbox": minor
---

`attnbox hooks --install`: one-command, idempotent setup of authoritative-mode hooks for Claude Code and Codex CLI — merges into existing configs with `*.attnbox-bak` backups, refuses unparseable configs instead of overwriting.

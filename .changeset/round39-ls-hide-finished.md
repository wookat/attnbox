---
"attnbox": patch
---

`attnbox ls` hides finished sessions (done/idle) by default with a "… N finished sessions hidden — `attnbox ls --all` to show" note; `--all` restores the full listing, `--json` always emits everything.

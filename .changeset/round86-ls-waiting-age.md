---
"attnbox": patch
---

`attnbox ls` now shows how long each waiting item has been waiting (compact age like `(5m)` / `(3h)` after the title) — the triage-priority signal the web inbox already had.

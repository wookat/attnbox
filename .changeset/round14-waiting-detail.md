---
"attnbox-core": minor
"attnbox-collectors": minor
"attnbox": minor
---

Waiting items now show what the agent is actually asking: Devin blocked sessions carry a `detail` preview (last agent message, cached by `updated_at`) rendered in the web inbox and as an indented line in `attnbox ls`.

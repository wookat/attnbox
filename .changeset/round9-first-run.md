---
"attnbox": patch
---

Web inbox: actionable first-run empty state (concrete commands for local/cloud setup and diagnosis) and an explicit "daemon connection lost — reconnecting" banner instead of silently freezing the list.

---
"attnbox": patch
---

The web inbox header now has a theme toggle (system / light / dark) — the default still follows your OS, and a manual choice persists in the browser with no flash on reload.

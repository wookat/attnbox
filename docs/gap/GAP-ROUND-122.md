# GAP-ROUND-122 — 第五批竞品扫描（纯文档）

Round 122. Driver dimension: competitor research — kookr every-round
check (per round-115 alert) plus new-entrant sweep.

## Found

- **kookr**: still merging multiple times a day (pty fd-leak fix,
  hot-path isolation) — all fleet-orchestration internals, no cloud
  aggregation. Watch continues.
- **ccmux**: TUI dialog polish only; no attention-surface movement.
- kelpie/coslash: quiet.
- Three same-day new entrants (all 0-star, Python, local-only,
  single-agent): david-claude-hud (macOS HUD), agent-inbox-dispatcher
  (agent-to-agent mailbox, different quadrant), cc-office (Mac Claude
  window live view). Notably, "who is waiting on you" is now the
  standard README phrase across new entrants — the category language
  has converged on our round-1 positioning.

Batch-six table in `docs/COMPARISON.md`.

## Verdict

No P0/P1; cloud aggregation + cross-agent unified view remains
uncontested. Docs-only; no changeset.

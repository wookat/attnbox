# GAP-ROUND-459 — 竞品第三十八批扫描（纯文档）

Round 459. Driver dimension: competitive scan
(watchlist full check + new-entrant sweep), first
since round-448.

## Watchlist (metadata + latest commits)

```text
claude-dispatcher (Innovology) → largest mover again:
  Cockpit v3 re-theme + plan→act→observe→ship chain +
  structured dispatch form; branch-from-default, one
  live dispatch per feature; Products lens polish.
  Still Claude-only local tmux cockpit, 9★.
agentfleet (beknazar)  → honest-status philosophy
  deepens ("report what a machine is actually doing,
  not what a timer implies"; collector stops
  misreporting pasted transcripts); fixes from first
  real cloud provision. Multi-machine local fleet, 0★.
jind-ai                → pane/adapter refactor wave
  continues (adapter setup state per call, pane
  identity fixes). ccmux quadrant, 1★.
kookr (kookr-ai)       → rapid pace continues:
  pipelineStarvation counters, cloud-metadata/
  link-local peer URL rejection (SSRF), quota
  headroom tuning, Escape/focus-trap a11y fix.
  Local router, no cloud aggregation, 3★.
ccmux (epilande)       → v1.3.0 tail polish only
  (pushed 08-08), 119★.
agent-inbox / trail-boss / kelpie / claude-notify
  → quiet (pushed 08-07/08-08, no new direction).
omnigent (omnigent-ai) → 8,432★, harness-quadrant,
  unchanged positioning.
```

## New-entrant sweep (three queries)

Only known repos returned (agent-inbox,
pulse-protocol, AgentPet, grove, tmux-agents-inbox,
agent-dash — all previously archived/observed). No
new local+cloud unified attention inbox found.

## Verdict

Differentiation unchanged (local+cloud unified
inbox, waiting reason/detail, action links, triage
workflow, honest source-specific semantics). No
P0/P1; docs-only, no changeset.

# GAP-ROUND-371 — 竞品第三十批扫描（纯文档）

Round 371. Driver dimension: competitor watch, first since
round-360. Real GitHub API evidence (repo metadata + last 5
commits per active watch, plus three-vector new-entrant search
restricted to repos created after 2026-08-06).

## Named watchlist

- **shariqh/agent-inbox** (1★) — still quiet since its
  2026-08-07 hardening burst; latest commits unchanged from
  round-360 (trusted-origin viewer mutations, network boundary,
  landing refresh). Still local-only, no cloud/Devin
  aggregation.
- **claude-dispatcher** (9★) — still the most active local
  rival: pushed 08-09 (build-version nag), plus model-family
  usage split and cockpit repo→product assignment.
- **kookr** (3★) — cadence continues at 08-07:
  pipelineStarvation counters, cloud-metadata/link-local peer
  URL rejection, quota headroom tuning, Sweep dialog focus
  trap.
- **ccmux** (119★) — v1.3.0 released 08-08; docs/tagline
  refresh, relay-skill trigger broadening. Still tmux/local.
- **agentfleet** — pushed 08-09: real machine-state reporting
  ("what a machine is actually doing, not what a timer
  implies") after first real cloud provision fixes.
- **jind-ai** (1★) — pushed 08-09: pane-identity plumbing
  refactors; tmux TUI lane unchanged.
- **trail-boss / claude-notify / kelpie / coslash** — pushed
  08-08 / 08-07 / 08-07 / 08-08 respectively (repo metadata);
  no lane change signals.

## New entrants

Three-vector search (created >2026-08-06): only agentfleet
itself (already watched) and an email-triage agent (different
domain) — no new direct entrant.

## Verdict

Differentiation unchanged — no rival aggregates local CLI +
cloud (Devin) attention. No P0/P1; docs-only, no changeset.

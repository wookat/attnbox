# GAP-ROUND-620：PWA 离线快照 + SSE 韧性复走——契约全部成立，无 P0/P1

日期：2026-08-10
驱动维度：PWA 离线快照 + SSE 断线/重连韧性（round-609 后首次；临时 daemon @ :4620，真实数据 ~4,200+ 会话——迄今最大）

## 实测结果

1. live 基线：94 卡，无 offline 指示。
2. 杀 daemon：94 卡全部保留 + offline 指示出现（不塌成骨架屏）。
3. 宕机中冷开新页：SW 快照恢复 94 卡 + offline 指示（离线可用）。
4. 重启 daemon：~8s 无刷新自动回 live（offline 指示消失，卡片保留）——本系列最快之一。
5. 页面 0 错误；daemon 日志 0 错误；杀净（端口 000），探针零残留。

## 方法注记

- 探针内 `pkill -f` 的模式若出现在探针自身或其包装 shell 的 cmdline 中会自杀；用 `dist/index\\.js` 正则转义规避，且 daemon 启动与探针执行须分开两次调用。

## 结论

- 无 P0/P1。纯文档轮：不改产品源码、不加 changeset。

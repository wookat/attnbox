# GAP-ROUND-277 — 竞品第二十一批扫描（纯文档）

Round 277. Driver dimension: competitor research — watchlist
sweep + new-entrant scan, first since round-267.

## Watchlist movements (window since round-267, ~1 day)

- **ccmux** — v1.3.0 shipped and docs refreshed (tagline for the
  OpenCode era, relay-skill trigger broadening, OpenCode
  float-second marker fix). Post-release polish; still tmux, no
  cloud.
- **kookr** — high frequency continues (pipelineStarvation
  counters, SSRF peer-URL rejection, quotaHeadroomThreshold,
  Sweep dialog focus trap). Still no cloud aggregation.
- **claude-dispatcher** — momentum continues after Cockpit v3:
  repos assignable to products from the cockpit, README
  screenshots rendered from a fictional fixture. Still
  Claude-only, dispatch-owned sessions.
- **coslash** — commit-source plumbing refinement only.
- **kelpie (misty-step)** — desktop sprint continues
  (thinking-level icons, keyboard nav, auto-reinstall); still
  herdr-bound.
- **herdr ecosystem** — herdr core shipped Windows support for
  all agent integrations; herdr-remote fixed HTTP push routing
  (actionable Telegram notifications landed 07-26);
  herdr-reviewr steady releases. The runtime's own
  attention/remote faces keep thickening.
- **agentfleet** — 404 mystery resolved: repo recreated fresh at
  beknazar/agentfleet (created 08-08) with the same pitch ("run
  a fleet of coding agents on remote machines... shows you which
  ones are blocked waiting on you") and its first real cloud-
  machine end-to-end provision fixes. Back on the named
  watchlist.
- **claude-notify** — quiet in this window (07-nothing new past
  Phase-4 hardening).

## New entrants (three targeted searches)

- **jind-ai (takaaki-s)** — tmux TUI for Claude/Codex/opencode:
  "see which agent is waiting on you", per-session worktrees,
  resume after reboot. Local-only, tmux-bound — ccmux quadrant.
- **trail-boss (jedarden)** — "single-pane attention router"
  daemon (systemd user service) surfacing whichever session is
  blocked waiting on you. Local-only; language directly in our
  lane, worth a named watch.
- **agent-inbox (shariqh)** — steady hardening (viewer mutation
  origin trust, startup migration serialization); still local
  MCP+SQLite, no cloud.

## Verdict

Movement stays in known quadrants (tmux TUIs, single-runtime
cockpits, herdr ecosystem, local attention routers). Two names
added to the watchlist (trail-boss, jind-ai) and agentfleet
re-pinned at its new home. Local+cloud unified inbox with
zero-intrusion discovery remains uncontested; no P2 trigger met.
No P0/P1; docs-only, no changeset.

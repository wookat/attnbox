# GAP-ROUND-481 — 竞品第四十批扫描（纯文档）

Round 481. Driver dimension: competitor scan, first
since round-470. Named-watch full sweep + three-way
new-entrant scan (authenticated `gh api`).

## Named watch

```text
claude-dispatcher (Innovology, 9★)
  → 仍动作最大：Cockpit v3 re-theme +
    plan→act→observe→ship 链 + 结构化 dispatch 表单
    (#29)、dispatch 信任继承 (#27)、默认分支切
    dispatch 分支/单 feature 单活跃 dispatch (#26)、
    补 CODE_OF_CONDUCT。仍 Claude-only tmux 工厂，
    无云端聚合。
agentfleet (beknazar)
  → 诚实状态哲学继续深化：collector 不再把粘贴的
    转录误报为 operator 指令 (#1)、"report what a
    machine is actually doing, not what a timer
    implies"、首次真实云机端到端 provision 后续修复。
ccmux (epilande, 119★)
  → v1.3.0 收尾期：README/文案打磨为主。
kookr → 8/7 后静默两日（此前高速迭代）。
kelpie (misty-step) → 低频维护。
agent-inbox (shariqh) → 8/7 后静默，仍纯本地。
coslash → 迁址 centauri-ai/coslash（3★），描述扩为
  会话重建+handoff brief——本地"注意力+移交"象限，
  无云端；盯防名单指向已更新。
trail-boss (jedarden)
  → ADR-1 落地：daemon 打包为 systemd user service、
    区分 daemon 不可达 vs 队列空——服务化继续。
impri → 迁址 sekera-radim/impri（1★）：定位收敛为
  "human-in-the-loop approval inbox for AI agents"
  （watchers + approval inbox、MCP + REST、可自托管），
  近期以 SEO 内容页发布为主。审批象限语言最近者，
  保持具名盯防；盯防名单指向已更新。
jind-ai (takaaki-s) → 高频重构继续（spawn/
  opencode setup 修复），tmux TUI 象限。
```

## New entrants

```text
kaprek (klausarent, 0★) → Claude Code 本地工作台：
  session viewer + chat + 计划触发 + 延迟审批队列。
  单 runtime 本地象限，入档观察。
anotify（多个疑似互刷仓，v0.2.1）→ 跨平台通知+审批
  workflow 工具；通知/审批象限，非聚合收件箱，存档。
三向搜索（unified attention inbox / agent attention
triage / approval inbox agents）首位命中仍是 attnbox
本仓，其余为已知仓或非同类，无新直接对手。
```

## Verdict

差异化不变：本地+云端统一收件箱、显式等待原因、
行动链接、分诊/ack、诚实状态语义仍无直接对手。
无 P0/P1；纯文档，无 changeset。

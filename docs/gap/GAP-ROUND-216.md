# GAP-ROUND-216 — 竞品第十五批扫描（纯文档）

Round 216. Driver dimension: competitor research — eight-repo
watchlist + new-entrant scan (first since round-207).

## Watchlist (window since round-207)

- **kookr** — resumed after two silent windows: pipelineStarvation
  counters in `kookr status`, rejection of cloud-metadata /
  link-local peer URLs (SSRF hardening continues), operator-tunable
  `quotaHeadroomThreshold`. Still local router, no cloud
  aggregation.
- **claude-dispatcher** — biggest mover: v2 cockpit ("eight live
  lenses", learned usage limits), worktree isolation + product
  grouping, **Windows support**, cockpit now the default UI.
  Still Claude-only, only covers sessions it dispatches.
- **ccmux** — post-v1.3.0 polish only (relay trigger phrasing,
  opencode timestamp fix, dialog styling). No new direction.
- **kelpie** — high-frequency desktop sprint continues (keyboard
  nav across agents, composer/model UI); still herdr-bound,
  single-operator.
- **agentfleet** — follow-up fixes from its first real cloud-machine
  end-to-end provision; remote multi-machine local plane maturing,
  observation priority holds.
- **coslash** — v0.0.1 follow-ups (amend detection, settings
  backend); local-only.
- **waiting-on-me / streamdeck-agents** — no pushes since late
  July.

## New-entrant scan

Searches for "waiting on you coding agents" / "agent attention
inbox" (created > 2026-08-01): no new relevant entrants — only
already-archived projects (claude-notify, nMn) and one Live2D
desktop-pet novelty (duty-on) with no inbox semantics.

## Differentiation check

Unchanged: no competitor spans local CLI + cloud vendor-authoritative
aggregation with a resident mobile-capable inbox. claude-dispatcher's
Windows + cockpit-default move deepens the single-runtime cockpit
lane, not the cross-runtime aggregation lane.

## Verdict

No P0/P1; docs-only, no changeset. P2 triggers untouched.

# GAP-ROUND-779：分诊全流程 UX 走查——契约全部成立

日期：2026-08-04
驱动维度：分诊全流程 UX（round-768 后首次）

## 走查证据（真实 daemon @4,427 会话，12 waiting）

- 默认视图 67 卡渲染、0 次全量 `/api/items` fetch（slim SSE 契约成立）。
- waiting 过滤 12 卡。
- 惰性搜索恰好 1 次全量 fetch；"attnbox" 在 All 过滤下 4 命中；负例（乱串）诚实空态 0 卡。
- 键盘链：j 选中 → e ack，台账 13→14；API 反 ack（`{id, at:null}`）1.8s 后台账逐字节还原至 13。
- ✓ all done：13→24 全部 ack；按基线快照逐条反 ack 精确还原至 13。
- `?` 帮助面板正常打开/Escape 关闭。
- 0 页面错误；daemon 日志零错误。

## 方法注记（探针假 FAIL，非产品缺陷）

- 搜索命中断言须先切回 All 过滤：waiting 过滤态下搜索只显示 waiting ∩ 命中（本轮 4 命中均非 waiting，显示 0 为正确行为）。首跑 1 处假 FAIL 由此产生，复核排除。

## 结论

11/11 有效断言全通（1 处为探针过滤态假设错误）。无 P0/P1。纯文档轮，探针零残留。

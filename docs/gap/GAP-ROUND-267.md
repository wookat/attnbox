# GAP-ROUND-267 — 竞品第二十批扫描（纯文档）

Round 267. Driver dimension: competitor research — nine-watch
sweep + new-entrant scan, first since round-256.

## Watchlist movements (window since round-256, ~2 days)

- **ccmux** — v1.3.0 released and tagline/README refreshed for
  the OpenCode era; post-release docs polish only. Still tmux,
  no cloud.
- **kookr** — high frequency continues: pipelineStarvation
  counters in `kookr status`, cloud-metadata/link-local peer-URL
  rejection (SSRF hardening), operator-tunable
  `quotaHeadroomThreshold` (0 disables the plan-quota gate).
  Still no cloud aggregation.
- **claude-dispatcher (Innovology)** — most active mover:
  Cockpit v3 shipped (command queue, mock data removed), repos
  assignable to products from the cockpit. Still Claude-only,
  local, dispatch-owned sessions.
- **claude-notify** — post-Phase-4 hardening continues (question
  text kept device-local, keyboard limits, honest turn state).
  Still Telegram + single runtime.
- **kelpie** — desktop sprint continues (thinking-level icons,
  keyboard navigation across agents, auto-reinstall on commit);
  still herdr-bound.
- **coslash** — commit-source plumbing refinement (amend
  detection scoping, heredoc/stdin handling).
- **herdr-island** — quiet since 08-04 (comment localization).
- **ProjectDispatcher / agentfleet** — silent (agentfleet repo
  now 404s — likely renamed/private; keep on watch via search).

## New entrants

Three targeted searches (waiting-on-you / attention inbox /
agent inbox) over the window: none.

## Verdict

All movement stays in known quadrants (single-runtime cockpits,
herdr-ecosystem attention faces, notify channels). Local+cloud
unified inbox with zero-intrusion discovery remains uncontested;
no P2 trigger met. No P0/P1; docs-only, no changeset.

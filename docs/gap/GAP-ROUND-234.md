# GAP-ROUND-234 — 竞品第十七批短窗复核（纯文档）

Round 234. Driver dimension: competitor scan — nine-repo
watchlist (round-226 added claude-notify) + new-entrant sweep,
short window since round-226.

## Watchlist (commits since 2026-08-08)

- **claude-dispatcher** — the only significant move: Cockpit v3
  merged — triage becomes a **command queue** (one blocker at a
  time, full width, rest queued beneath; `w` working view grouped
  by product; empty prompt dispatches typed commands), and mock
  portfolio data removed from the product. This is a
  triage-ergonomics leap on the local Claude-only plane; the
  single-runtime, no-cloud boundary is unchanged. Keep elevated
  watch.
- **ccmux** — v1.3.0 release wrap-up (tagline/README refresh
  only).
- **agentfleet** — same post-provision fixes already recorded in
  round-226; nothing new after.
- **kookr / coslash / kelpie / waiting-on-me / streamdeck-agents
  / claude-notify** — silent in this window.

## New entrants

Sweep across "waiting on you" / "agent waiting inbox" /
"attention coding agents" (created >2026-08-07): nothing new
beyond agentfleet (already on file).

## Verdict

Differentiation unchanged: local+cloud unified attention inbox
with zero-intrusion discovery remains uncontested;
claude-dispatcher's command-queue triage is the pattern to watch
on ergonomics. No P0/P1; docs-only, no changeset.

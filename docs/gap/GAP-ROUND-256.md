# GAP-ROUND-256 — 竞品第十九批扫描（纯文档）

Round 256. Driver dimension: competitor scan — nine-repo
watchlist + new-entrant sweep, first since round-245.

## Watchlist

- **ccmux** — the notable move: **v1.3.0 shipped** (docs/tagline
  refreshed for the attention+handoff positioning), the relay
  skill split out of dispatch with broadened trigger phrasing +
  a `y` hotkey, and an OpenCode marker-timestamp fix confirming
  **OpenCode is now a monitored runtime** alongside Claude —
  ccmux keeps widening local-runtime coverage, still tmux-bound,
  no cloud agents.
- **claude-notify** — post-Phase-4 hardening only (question text
  kept on-machine, keyboard withdrawn at turn end) — already on
  file from round-245; no new capability since.
- **kookr** — steady tempo (SSRF peer-URL rejection continued,
  pipelineStarvation counters in status, tunable quota gate);
  still local-only.
- **kelpie (misty-step)** — desktop sprint continues (keyboard
  nav across agents, icons/focus ring) + a VISION.md; herdr
  ecosystem only.
- **coslash** — commit-source plumbing refinements only.
- **claude-dispatcher / agentfleet / waiting-on-me /
  streamdeck-agents / herdr-island** — silent this window.

## New entrants

Sweep across "agent attention inbox" / "waiting on you agent"
(created >2026-08-04): nothing new beyond repos already on file.

## Verdict

Differentiation unchanged: local+cloud unified attention inbox
with zero-intrusion discovery remains uncontested. ccmux v1.3.0
adding OpenCode widens its local coverage but stays in the
tmux-local quadrant. No P0/P1; docs-only, no changeset.

# GAP-ROUND-144 — 第八批竞品扫描（纯文档）

Round 144. Driver dimension: competitor research — every-round watch
(kookr, agent-deck, ccmux, coslash) + entrant sweep (first since
round-138).

## Found

- Watchlist quiet: kookr / agent-deck / ccmux / coslash have had no
  pushes since the states verified in round-138; kelpie still 404.
- Three new entrants:
  - **herdr-island**: a "find the agents waiting on you" filter
    plugin for herdr (25.5k-star meta runtime) — the strongest signal
    yet that attention filtering is becoming a first-class dimension
    inside major agent runtimes (same direction as omnigent's builtin
    Inbox). Ecosystem-bound: only herdr-launched sessions.
    - **nMn**: auto-confirms Claude Code permission prompts in VS Code
    — the opposite philosophy (eliminate waiting instead of surfacing
    it; auto-approval, questionable safety), single agent.
  - **claude-notify** (new author, same name as the round-98 entry):
    Telegram pings when Claude Code waits, only after you actually
    step away — another presence-aware implementation (round-98 P2
    observation direction keeps being validated independently).

Batch-eight section in `docs/COMPARISON.md` (四之十).

## Verdict

Local rivals quiet; the ecosystem signal (runtime-native attention
surfaces) is the thing to keep watching — it can erode single-runtime
users' need for an aggregator, while cross-runtime + native + cloud
unified aggregation remains uncontested. No P0/P1; docs-only, no
changeset.

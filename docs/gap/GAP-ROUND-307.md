# GAP-ROUND-307 — 竞品第二十四批扫描（纯文档）

Round 307. Driver dimension: competitor research — full
watchlist check + new-entrant scan, first since round-298.
Window: commits since 2026-08-03.

## Watchlist (all checked)

- **claude-dispatcher** (Innovology) — still the most active
  local rival: Cockpit v3 follow-through — repo→product
  assignment from the cockpit, usage split by model family,
  README screenshots from fixtures. Local-only, no cloud agents.
- **herdr** — heavy Windows push (all agent integrations on
  Windows, atomic installer swaps), per-pane right-click
  routing, terminal bell forwarding. Terminal-mux quadrant.
- **herdr-island** (kay-ws) — post-launch hardening only:
  reason-clearing fix, macOS support, comment anglicization. No
  scope expansion beyond herdr plugin.
- **ccmux** — v1.3.0 released; docs/tagline refresh, relay-skill
  trigger phrasing broadened, OpenCode marker fix. Still
  tmux-bound, local only.
- **claude-notify** — post-Phase-4 hardening: turn-state honesty
  ("stop lying about the turn"), question-text privacy option.
  Telegram + one local machine.
- **kookr** — steady platform cadence (pipeline starvation
  counters, SSRF/link-local peer URL rejection, quota headroom
  tuning). Orchestrator quadrant.
- **kelpie** — desktop app iteration (keyboard nav across
  agents, thinking-level icons, VISION.md). herdr-adjacent.
- **coslash** — commit tooling refinement + first README. Not
  attention-inbox scoped.
- **agentfleet** — first real cloud-machine provision fixes
  landed; fleet-runner quadrant, no waiting-reason surface.
- **trail-boss** — license added, systemd ADR landed earlier;
  daemon-unreachable vs queue-empty distinction. Local router.
- **jind-ai** — worktree/state-dir fixes, send-to-blocked-session
  matured. tmux TUI quadrant.
- **YuruPager** — repo still empty (HTTP 409). Archived watch.
- **claude-mission-control** — fast solo iteration (chat
  assistant creates tasks, in-app notification bell, calendar
  reminders). Single-framework (Claude only), local.
- **duty-on** — Live2D pet added Codex/OpenCode support +
  Cursor IDE emphasis. Notifier quadrant, no inbox.

## New entrants

- **RajdeepKushwaha5/nMn** — VS Code auto-approval switch for
  Claude Code permission prompts ("doesn't stall waiting on
  you"). Opposite philosophy to an attention inbox (auto-approve
  rather than surface); single-framework, v0.1, archived — not
  watched.

No other new entrant matches the attention-inbox category.

## Verdict

Differentiation unchanged: no entrant aggregates local CLI +
cloud agents into one attention inbox with vendor-authoritative
cloud status. No P0/P1; docs-only, no changeset.

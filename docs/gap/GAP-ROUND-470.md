# GAP-ROUND-470 — 竞品第三十九批扫描（纯文档）

Round 470. Driver dimension: competitive scan, first
since round-459. Named watchlist checked via GitHub
API (repo metadata + latest commits) plus three-way
new-entrant searches ("attention inbox agents",
"waiting on you coding agents", "agent approval
inbox", "unified inbox coding agents").

## Watchlist status (as of 2026-08-10)

```text
claude-dispatcher → most active again: "Cockpit v3:
  re-theme, the plan→act→observe→ship chain, and a
  structured dispatch form" + repo-trust inheritance
  for dispatch start; still Claude-only local tmux
agentfleet        → honest-status philosophy keeps
  deepening: "Report what a machine is actually
  doing, not what a timer implies" + collector fix
  to stop mistaking a pasted transcript for the
  operator's instruction; remote fleet, no waiting
  reason/ack triage
kookr             → high-velocity continues (SSRF
  peer-URL rejection, pipelineStarvation counters,
  plugin-dir contract tests); local router, no cloud
ccmux             → v1.3.0 wrap-up (docs/tagline
  refresh); tmux local only
jind-ai           → steady tmux TUI test/refactor
  stream; ccmux quadrant
trail-boss        → moved to systemd user service +
  honest "daemon unreachable vs queue empty" status;
  language kin ("single-pane attention router"),
  local only
switchyard        → worktree/task-repo iteration;
  dispatcher quadrant
HumanLoop         → device pairing + Cursor approval
  bridge + binding-scoped alerts; approvals only,
  no waiting inbox surface
agent-inbox(shariqh)/kelpie → quiet since 08-07
```

## New entrants

```text
impri (sekera-radim)  → "Approval Inbox for AI
  Agents", cloud API + hosted inbox, human-in-the-
  loop imprimatur queue. Closest new language in the
  approvals quadrant; agent-initiated action
  approval, not an aggregated who-is-waiting inbox
  across local CLI + cloud vendors. Named-watch.
gnestor/agent-inbox   → unified inbox for emails,
  Notion tasks, and Claude Code sessions; personal
  productivity aggregation, single agent runtime.
  Archive-watch.
anotify repos         → templated README farms
  (multiple near-identical repos), not a real
  product signal. Ignored.
```

## Verdict

Differentiation unchanged: no competitor combines
local CLI + cloud agents in one inbox with explicit
waiting reason/detail, action links, and ack triage.
No P0/P1; docs-only, no changeset.

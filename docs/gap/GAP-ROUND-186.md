# GAP-ROUND-186 — 第十三批竞品扫描（纯文档）

Round 186. Driver dimension: competitive research — watchlist
(kookr / ccmux / coslash / kelpie / claude-dispatcher) + new-entrant
scan (first since round-176). All via authenticated GitHub API,
commits since 2026-08-04.

## Watchlist (all five active)

- **kookr-ai/kookr** (3★, pushed <1 h ago): unabated pace —
  `quotaHeadroomThreshold` operator tunable, FAA ack-path reaper
  with operator veto, node-pty FD_CLOEXEC leak fix, Sweep dialog
  focus trap. Still no cloud-agent aggregation.
- **epilande/ccmux** (117★): relay skill trigger phrasing broadened,
  opencode float-second timestamps, dialog styling unification.
- **centauri-ai/coslash** (2★): pivoted into git-commit monitoring
  detail (amend detection, heredoc handling). Local-only.
- **misty-step/kelpie** (1★): desktop sprint continues — keyboard
  navigation across agents, Codex-desktop-parity composer. Still
  herdr-bound.
- **Innovology/claude-dispatcher** (9★): Windows support, cockpit
  made the default, Homebrew release pipeline. Claude-only.

## New entrants (three, all "waiting/blocked on you" language)

- **beknazar/agentfleet** (0★, created today, Shell): control plane
  for coding agents on *remote machines* — each machine computes
  status locally into a JSON file, laptop aggregates one line per
  machine sorted by "blocked on you". Closest multi-machine local
  competitor yet; no vendor cloud API aggregation.
- **dkomlen/waiting-on-me** (0★): Claude Code skill — one command
  concurrently scans seven sources (your PRs, reviews blocked on
  you, Linear, Slack, background tasks, idle agent sessions,
  memory) into a one-screen verdict. Same cross-source attention
  thesis, but pull-based one-shot glance, not a live inbox.
- **chron/streamdeck-agents** (0★): Stream Deck keys as peripheral
  vision for blocked Claude sessions (amber = waiting). Hardware
  novelty, Claude-only, single machine.

## Assessment

Track validation keeps strengthening — every new entrant this
window uses our round-1 language. Nobody combines cross-runtime
local discovery + vendor-authoritative cloud aggregation + a
persistent mobile-first inbox. No P2 trigger; no P0/P1; docs-only,
no changeset.

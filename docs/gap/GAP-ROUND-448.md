# GAP-ROUND-448 — 竞品第三十七批扫描（纯文档）

Round 448. Driver dimension: competitive scan, first
since round-437. Watchlist full check + new-entrant
sweep (GitHub search, created ≥2026-08-01).

## Watchlist movements

```text
claude-dispatcher (Innovology, ★9, pushed 08-09)
  → still the most active local rival: Cockpit v3
    re-theme + plan→act→observe→ship chain + structured
    dispatch form; dispatch trust inheritance, branch
    hygiene (one live dispatch per feature). Still
    Claude-only tmux factory cockpit, no cloud.
agentfleet (beknazar, pushed 08-09)
  → honest-status philosophy deepens: "report what a
    machine is actually doing, not what a timer
    implies"; collector fix to not misreport pasted
    transcripts as operator instruction; fixes from
    first real cloud provision. Multi-machine local
    fleet face closest, still no waiting-reason triage.
kookr (kookr-ai, ★3, pushed 08-07)
  → high-velocity iteration continues (2,180+ PRs):
    starvation counters in status, SSRF peer-URL
    rejects, quota gate tuning. Local router only.
ccmux (epilande, ★119, pushed 08-08)
  → v1.3.0 released, docs polish + relay-skill trigger
    broadening. tmux-local, no cloud.
jind-ai (takaaki-s, ★1, pushed 08-09)
  → pane/adapter refactor stream continues. ccmux
    quadrant.
agent-inbox (shariqh, ★1, pushed 08-07) → quiet after
  hardening wave; still purely local.
trail-boss (jedarden, pushed 08-08) · kelpie
  (misty-step, 08-07) · claude-notify (oleg-vasilyev,
  08-07) · coslash (centauri-ai, 08-08) → routine or
  quiet, no quadrant change.
duty-on / switchyard / HumanLoop → original repo paths
  404 on unauthenticated API this window; search shows
  no active successor under those names claiming our
  space — keep archived-watch status.
```

## New entrants (three-vector search)

Only prior-known repos surfaced (herdr-island, nMn,
agentfleet, claude-notify); one off-domain
support-ticket agent. No new direct competitor to the
local+cloud unified attention inbox with
waiting-reason + action links + triage.

## Verdict

Differentiation unchanged. No P0/P1; docs-only, no
changeset.

# GAP-ROUND-724：分诊全流程 UX 走查——契约全部成立

日期：2026-08-04
驱动维度：分诊全流程 UX 走查（round-713 后首次；搜索→过滤→ack all→反 ack + 键盘链）

## 证据（@4,350 会话）

- 默认态 0 次全量 /api/items fetch（slim SSE 契约成立）。
- 惰性搜索恰好 1 次 fetch，"devin" 4,344 命中；负例诚实空态 0 卡。
- j/e 键盘 ack：台账 13→14；API 反 ack（`at: null`）后台账逐字节还原。
- Needs you 过滤 + "✓ all done"：台账 13→21；逐项反 ack 后逐字节还原。
- ? 帮助面板正常开合。
- 0 页面错误、daemon 日志零错误，探针零残留（daemon 已停、台账逐字节还原核验）。

## 方法注记（新增）

- ack-all 断言须考虑"已 ack 但重回 waiting"的存量项：ack all 会以新时间戳合法重写其台账条目，探针还原不能只反 ack 新增 ID，须按基线快照恢复原时间戳（本轮 1 处假 FAIL 已排除，非产品缺陷——重 ack 属真实重转换的忠实行为）。

## 结论

- 无 P0/P1。纯文档轮：不改产品源码、不加 changeset。

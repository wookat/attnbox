# GAP-ROUND-437 — 竞品第三十六批扫描（纯文档）

Round 437. Driver dimension: competitor watch (named
watchlist + new-entrant sweep), first since round-426.
Authenticated `gh api` / `gh search repos`.

## Watchlist

- `Innovology/claude-dispatcher` (9★, pushed today) — again
  the most active: Cockpit v3 re-theme with a
  plan→act→observe→ship chain + structured dispatch form,
  dispatch branches cut from default branch (one live
  dispatch per feature), repo-trust inheritance so
  dispatches actually start. Still Claude-only local
  cockpit; no cloud-agent aggregation.
- `beknazar/agentfleet` (0★, pushed today) — honesty
  deepens: "report what a machine is actually doing, not
  what a timer implies"; collector stops mistaking pasted
  transcripts for operator instructions; first real
  cloud-machine provision fixed end-to-end. Remote-fleet
  quadrant, no triage/ack model.
- `philontos/switchyard` (9★) — steady worktree/tmux
  dispatcher iteration (repo details view, task repo refs,
  Kimi support). Dispatcher quadrant.
- `kookr-ai/kookr` (3★) — active, attention-router framing
  unchanged.
- `epilande/ccmux` (119★) — steady, tmux-local.
- `shariqh/agent-inbox` (1★) — quiet since 08-07, still
  local MCP+SQLite.
- `marine841023/duty-on` (2★), `Revolper/HumanLoop` (0★) —
  quiet; neither grew a waiting-reason/triage surface.

## New entrants

Sweeps across "agent inbox / waiting-on-you / HITL notify"
(created ≥2026-08-01): only agent-to-agent mailboxes,
workflow docs, and a macOS Codex pet — none are a
human-facing cross-agent attention inbox. No direct
competitor.

## Verdict

Differentiation unchanged (local+cloud unified inbox,
honest waiting reasons, action links, triage/ack, mobile).
No P0/P1; docs-only, no changeset.

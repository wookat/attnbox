# Handoff context — attnbox

按 Company OS 交接上下文制度维护；换会话/换负责人时先读本文档。最后更新：2026-08-09（ROUND-313）。

## 项目一句话

跨"本地 CLI agent + 云端 agent"的统一注意力收件箱：不管 agent 跑在哪里，"哪个在等我、等我做什么（审批/回答问题/审 PR）"聚合成一个视图。

## 仓库与服务

- 仓库：https://github.com/wookat/attnbox （main 分支保护，PR + CI）
- 官网：https://attnbox.zalize.com （Astro Starlight，`apps/site`，Cloudflare Pages，项目名 `attnbox`；文档 PR 合并后需 `pnpm build` + `wrangler pages deploy dist` 重建）
- npm：`attnbox`（CLI+web）、`attnbox-core`、`attnbox-collectors`、`attnbox-daemon`；当前 0.4.8 / 0.2.1 / 0.2.7 / 0.4.0（v0.4.8 Release 已建，round-125 slim SSE）
- monorepo：pnpm + changesets；`packages/{core,collectors,daemon,cli}` + `apps/{web,site}`

## 日常循环（当前运作方式）

1. 每轮（ROUND-N）轮换主驱动维度：真实测试 / UX 走查 / 前端视觉 / 竞品调研 / 用户与数据分析 / 文档新鲜度。
2. 有 P0/P1 → 修复 + focused tests + changeset（包行为变更时）；无 → 纯文档 GAP 轮。
3. 每轮产出 `docs/gap/GAP-ROUND-N.md`，独立分支 + PR，CI 绿后由总负责人合并。
4. 文档 PR 合并后重建官网；changeset 累积到值得发版时报总负责人出版本 PR。
5. 发布顺序 core→collectors→daemon→cli，逐包 `pnpm pack` + `npm publish <tarball>`；发布后干净环境回归 + GitHub Release。

## 质量门禁

`pnpm lint && pnpm typecheck && pnpm build && pnpm test`（98 测试，coverage 阈值 CI 强制）。

## 关键设计事实（容易踩的坑）

- Devin 采集：全量分页爬取（100/页，深页 10 并发批，硬上限 10,000，30s 深页缓存）；云状态权威透传，5 分钟 stale-working 上限仅本地启发式适用。
- Devin waiting 详情：一次 collect 取全（顺序批，每批 10 并发，`updated_at` 缓存）——round-111 修复，勿回退为每周期条数上限（一次性 `ls` 永不补齐）。
- a11y 审计要覆盖交互态：round-117 的激活 tab 徽章、round-119 的内联代码底色都是默认态审计漏掉的（axe/Lighthouse 只测默认 tab/白底就会漏）。3k done 卡上直接跑 axe 会耗尽本机内存（round-147）：done 态固定审前 60 卡（覆盖全部卡片变体）。
- webhook/浏览器通知：id 仅在观测到非 waiting 后才离开已通知集合（防采集器抖动风暴，rounds 71/81）。
- `--host` 必须 token（`ATTNBOX_TOKEN`），API/SSE 全面 401 门禁（rounds 29/84 负例契约）。
- doctor 的 GitHub 探活走 review-requested 搜索端点，不能用 `/user`（App token 会 403，round-94）。
- 离线快照走 localStorage（round-79 重构过写路径，勿回退为每 tick 同步写；round-125 起恢复也是惰性的——SSE 快照先到就跳过缓存解析，勿回退为启动即解析）。
- 通知探针假 0 读数：`attnbox:notify` localStorage 残留 `off` 会静默关闭整条通知路径（`Notification.permission` 仍 granted），round-221 前两个观察窗因此读到 0——通知探针必须先核实 bell aria-pressed=true（或 notify-ls ≠ off）再信任零读数，跑完还原状态。
- API 反 ack 契约是 `POST /api/ack {id, at: null}`：探针发 `{acked:false}` 会被输入校验拒绝（缺 `at`），"反 ack"静默不生效造成台账残留——round-233 捕获，批量清理走 `{id, at:null}` 循环并核实顶层 acked map 归零。
- 3.4k+ 规模 `ls --all --json` 输出会打爆 execSync 默认缓冲（ENOBUFS，round-265）——探针 shell 出全量 CLI 时必须传大 `maxBuffer`（如 128MB）。
- detached daemon 相对路径按 shell cwd 解析（round-286 一次 MODULE_NOT_FOUND）——`setsid nohup node ...` 起探针 daemon 时 CLI 路径用绝对路径。
- axe 审计 done 标签页后进搜索态要先 reload——done tab 不渲染搜索框，直接 locator 会超时（round-284）。
- 长活跃会话的探针标签页要每轮收尾关闭：数周探针遗留 ~50 个 localhost/about:blank 标签页会把共享 Chrome 压垮（load 75、单 renderer 1.3GB、kswapd 抖动，round-308 dark 主题首跑卡死）——用 CDP `/json/close` 清空后一分钟内恢复。
- 离线/重连断言不能用全页文本 regex——dogfood 规模下会话卡片文本可能自带 "--offline" 等词造成假阴性（round-297 首跑）——断言 header live/offline pill 与精确横幅文案（"Connection to the attnbox daemon lost"）。
- 实机探针要独占端口：round-149 一次误读源于前一探针遗留的同端口第二 daemon——起测前 `pgrep -f "port <N>"` 清场。一次性 shell 起探针 daemon 必须 `setsid nohup ... < /dev/null`，裸 `nohup &` 会随父 shell 死掉造成假 "RSS 0" 读数（round-203/210）——采样前先 `pgrep -f` 核实 PID 存活。
- slim SSE（round-125）：web 订阅 `/api/events?slim=1`，事件不含 done 项，summary 仍全量；Done tab/搜索/分组/展开器靠 `/api/items` 惰性取 done（`summary.total - items.length` 漂移即失效重取）。改 web 视图逻辑时注意用 `allItems`（含惰性 done）而非 `data.items`。

## 已知 P2 台账（勿擅自实现，触发条件见各 GAP）

SSE delta 事件、payload 去重/归一、展开 3k 列表虚拟化、本地 agent 远程审批（零侵入约束）、presence-aware 通知（round-98 观察项）、Devin project 归组覆盖率 41.6%（受供应商字段限制）、waiting 紧急度排序/解释与交接简报（round-109 观察项，触发：dogfood waiting 多到时长排序不够用）。round-106 的性能升级触发已于 round-125 命中并落地（slim SSE，payload 瘦身）；剩余虚拟化仅在 slim 后仍可复现 perf<70 时再议。

## 资源与凭证

- `DEVIN_API_KEY`（环境已配）；GitHub 走内置认证；Cloudflare wrangler 已登录。
- cursor-agent 登录凭证仍缺（多轮资源续报中）；opencode 本机无真实数据。

## 进行中/下一步

- 无未合并 PR 时按循环继续；竞品每轮必查 kookr（迭代速度极高，round-115 示警；round-168 单日 15+ 提交；round-176 SSRF 硬化仍无云端）+ ccmux（round-256：v1.3.0 正式发布并纳管 OpenCode——relay skill 独立拆分、OpenCode 成为受监控 runtime，本地覆盖面扩宽仍 tmux 无云端；round-207 发 v1.3.0 预备：session handoff 正式化——`ccmux last`/`ccmux handoff`、relay 技能 agent 互移交、worktree 面板；本地"注意力+移交"路线深化，仍单机 tmux 无云端）+ coslash（round-168 发 v0.0.1 README，定位原话 "attention layer for coding agents"，仅本地）+ herdr 系（misty-step/kelpie round-176 发 VISION.md——"the operator's scarce resource is attention, not terminal windows"，与我们 round-1 注意力定位语言同源，明确单人/herdr 生态内/非托管）+ claude-dispatcher（round-176 入档：Claude-only tmux 工厂驾驶舱，仅覆盖它 dispatch 的会话；round-186 上 Windows + cockpit 默认化；round-267 Cockpit v3 命令队列+去 mock+驾驶舱内 repo→产品分配，当前动作最大的本地对手）；round-245 关键信号：claude-notify Phase 4 落地"从 Telegram 远程回答 Claude 提问"（notify→answer 跨越，我们本地 agent 远程审批 P2 观察项的野生实现，仍 Claude-only 单 runtime 不重合聚合核心，已升为常规盯防）；round-186 新进入者三个入档：agentfleet（远程多机 fleet 控制面，多机本地面最接近者）、waiting-on-me（七源并发扫的 Claude 技能，pull 式 glance）、streamdeck-agents（硬件外设化提醒）；agentfleet round-197 落第一次真实云机端到端 provision，round-267 原仓 404 后 round-277 于 beknazar/agentfleet 全新重建并落首次真实云机 provision——复位具名盯防；round-277 新进入者两个：trail-boss（"single-pane attention router" daemon，语言同道，升具名盯防）、jind-ai（tmux TUI，ccmux 象限）；kookr round-207 首次连续两窗口静默；agent-deck 已 404 退出（round-153，继初代 kelpie 后第二个）；round-159/168 连续两个三日窗口新进入者扫描为空；round-144 起关注生态信号——herdr（25.5k★）等头部 runtime 生态出现 waiting-on-you 官方式插件，runtime 自带注意力面会蚕食单 runtime 用户的聚合需求。ccmux 已修 Escape 陈旧 waiting 并上线 session handoff（round-138）——两个本地对手都在向"注意力+行动/移交"演进，验证 round-109 交接简报观察项方向；其信号依赖 capture-pane，零侵入约束下我们不可用，round-72 不修决策成立。
- changeset 当前无累积（v0.4.8 已清空）。
- Rounds 300–312 概要（全部纯文档、无 P0/P1）：300 交接整备（补 289–299 收敛 + offline 断言方法注记）、301 数据面 3,479 全干净（waiting 20/20、中位 11.2 分钟、连续第十四个干净数据轮）、302 CLI 黄金路径全通（ls --waiting 3.2s@3,480、hooks 沙箱四态）、303 合并面 soak 回归全绿（15 分钟 @3,488 迄今最大 RSS 96→~136MB 平稳零错误、双主题 smoke 0 错误）、304 分诊全流程复走全通 @3,488（默认态 0 全量 fetch、惰性搜索恰好 1 次、j/e ack 往返、✓ all done 11 项 + API 反 ack 归零；坑：快捷键前先 blur 搜索框）、305 本地采集器实弹三采集器全对（零残留）、306 MATURITY 证据刷新至 rounds 293–305、307 竞品第二十四批（claude-dispatcher 保持动作最大——Cockpit v3 收尾 repo→产品指派+按模型族用量拆分；herdr 大推 Windows；新进入者仅 nMn——VS Code 自动批准 Claude 权限弹窗，相反哲学，存档不盯防）、308 axe 复审 10 态 0 违规（与历史十二轮持平；宿主坑见上：陈旧探针标签页压垮 Chrome）、309 --host 门禁七面+webhook 复测（17 存量零误 POST、3.5 分钟恰好 5 POST 五唯一 ID 零重复全为真实转换）、310 PWA 离线+SSE 韧性复走全通（81 卡保留、宕机冷开快照恢复、重启 ~5s 回 live 追平最快）、311 数据面 3,555 全干净（迄今最大规模；waiting 22/22、中位 10.4 分钟、连续第十五个干净数据轮）、312 CLI 黄金路径全通（doctor 七行、ls --waiting 3.0s@3,555、hooks 沙箱四态）。
- Rounds 289–299 概要（全部纯文档、无 P0/P1）：289 竞品第二十二批（trail-boss daemon systemd 服务化；新进入者仅 ryu-approvals 单框架存档）、290 分诊全流程复走全通 @3,452（默认态 0 全量 fetch、惰性搜索恰好 1 次、✓ all done 25 项 + API 反 ack 归零）、291 本地采集器实弹三采集器全对（agent+stamp 双过滤、零残留）、292 MATURITY 证据刷新至 rounds 281–291、293 合并面 soak 回归全绿（15 分钟 @3,463 迄今最大 RSS 109–146MB 零错误、双主题 smoke 0 错误）、294 数据面 3,464 全干净（waiting 17/17、中位 17.5 分钟、连续第十三个干净数据轮）、295 CLI 黄金路径全通（ls --waiting 3.0s@3,466、hooks 沙箱四态）、296 axe 复审 10 态 0 违规（与历史十一轮持平）、297 PWA 离线+SSE 韧性复走全通（65 卡、~10s 回 live；坑见上：offline 断言用 pill/横幅精确选择器）、298 竞品第二十三批（herdr 生态新插件 herdr-island——"waiting on you"+停顿原因直显，waiting-reason 语言持续扩散并入 herdr 盯防注记；新进入者 YuruPager/claude-mission-control/duty-on 均单框架或空仓存档）、299 --host 门禁七面+webhook 复测（19 存量零误 POST、3.5 分钟恰好 2 POST 合法转换 0 重复）。
- Rounds 274–287 概要（全部纯文档、无 P0/P1）：274 axe 复审 10 态 0 违规（与历史九轮持平）、275 --host 门禁七面+webhook 复测（20 存量零误 POST、3.5 分钟 1 POST 合法转换）、276 PWA 离线+SSE 韧性复走全通（66 卡、~35s 窗口回 live；首个无效探针运行按方法注记作废重跑）、277 竞品第二十一批（agentfleet 404 之谜解开——beknazar/agentfleet 全新重建+首次真实云机 provision 复位具名盯防；新进入者 trail-boss——"single-pane attention router" daemon 语言同道升具名盯防、jind-ai——tmux TUI ccmux 象限；herdr 生态注意力/远程面增厚）、278 分诊全流程复走全通 @3,415（负例诚实空态、✓ all done 8 项 + API 反 ack 归零）、279 本地采集器实弹三采集器全对（agent+stamp 双过滤、二次全量复核零残留）、280 MATURITY 证据刷新至 rounds 267–279、281 合并面 soak 回归全绿（15 分钟 @3,430 RSS 108–145MB 零错误、双主题 smoke 0 错误）、282 数据面 3,433 全干净（waiting 16/16、中位 20.9 分钟、连续第十二个干净数据轮）、283 CLI 黄金路径全通（ls --waiting 2.8s@3,444、hooks 沙箱四态）、284 axe 复审 10 态 0 违规（坑见上：done 后搜索态需 reload）、285 移动端 Lighthouse 五跑 perf 中位 94/TBT ≤43ms @3,449（历史最大规模，与八轮持平）、286 --host 门禁七面+webhook 复测（20 存量零误 POST、恰好 2 POST 合法转换；坑见上：绝对路径）、287 PWA 离线+SSE 韧性复走全通（81 卡、~45s 窗口回 live）。
- Rounds 261–272 概要（全部纯文档、无 P0/P1）：261 数据面 3,389 全干净（waiting 25/25、中位 18 分钟、连续第十个干净数据轮）、262 axe 复审 10 态 0 违规（与历史八轮持平）、263 CLI 黄金路径全通（ls --waiting 2.8s@3,390）、264 PWA 离线+SSE 韧性复走全通（75 卡、重启 ~5s 回 live 追平最快）、265 本地采集器实弹三采集器全对（坑见上：ENOBUFS/maxBuffer）、266 MATURITY 证据刷新至 rounds 255–265、267 竞品第二十批（claude-dispatcher Cockpit v3 命令队列+去 mock 动作最大；ccmux v1.3.0 后仅打磨；agentfleet 仓库 404 疑改名/转私有，改走搜索盯防；三向新进入者扫描为空）、268 合并面 soak 回归全绿（15 分钟 @3,403 RSS 103–134MB 零错误、双主题 smoke 0 错误）、269 分诊全流程复走全通 @3,400（惰性搜索恰好 1 次独立探针复核、✓ all done 16 项 + API 反 ack 归零）、270 移动端 Lighthouse 五跑 perf 全 94/TBT ≤33ms @3,404（历史最大规模，与七轮持平）、271 数据面 3,408 全干净（连续第十一个干净数据轮）、272 CLI 黄金路径全通（ls --waiting 3.3s@3,409、hooks 沙箱四态）。
- Rounds 248–259 概要（全部纯文档、无 P0/P1）：248 axe 复审 10 态 0 违规（与 147/162/178/196/206/219/236 持平）、249 通知路径实测（12 存量零通知风暴防护成立，5 分钟 4 次真实转换恰好 4 条 SW 通知 0 重复；坑：首个观察窗因通知权限未开作废重跑——须先核实 permission=granted 且 bell 开启）、250 数据面 3,331 全干净（waiting 13/13、中位 16.8 分钟、连续第九个干净数据轮）、251 CLI 黄金路径全通（ls --waiting 3.1s@3,345、30 waiting 迄今最大集）、252 PWA 离线+SSE 韧性复走全通（102 卡；坑见上：execSync 起后台 daemon 挂探针，重启走独立 shell）、253 本地采集器实弹三采集器全对（坑：断言须 agent+stamp 双过滤防跨 agent 误匹配）、254 MATURITY 证据刷新至 rounds 244–253、255 移动端 Lighthouse 五跑 perf 全 94/TBT 0–2ms @3,371（round-239 增益保持；坑：Lighthouse 找不到 Chrome 时置 CHROME_PATH=Playwright chromium）、256 竞品第十九批（ccmux v1.3.0 发布并纳管 OpenCode——本地覆盖扩宽仍 tmux 无云端；claude-notify 仅 Phase 4 后加固）、257 合并面 soak 回归全绿（15 分钟 @3,381 RSS 125–140MB 零错误、双主题 smoke 0 错误）、258 分诊全流程复走全通 @3,380（✓ all done 24 项 + API 反 ack 归零）、259 --host 门禁七面+webhook 复测（29 存量零误 POST、3.5 分钟 5 POST 全为合法新转换 0 重复）。
- Rounds 236–246 概要（全部纯文档、无 P0/P1）：236 axe 复审 10 态 0 违规（与 147/162/178/196/206/219 持平）、237 --host 门禁六面+webhook 复测（13 存量零误 POST、3 分钟 3 POST 全为合法新转换 0 重复）、238 数据面 3,301 全干净（waiting 15/15 detail+url+attention、中位 8.8 分钟、连续第八个干净数据轮）、239 移动端 Lighthouse 五跑 perf 全 94/TBT 0–77ms @3,301、240 CLI 黄金路径全通（ls --waiting 3.9s@3,301、20 waiting；hooks 沙箱四态含缺目录守卫）、241 PWA 离线+SSE 韧性复走全通（82 卡、重启 ~10s 回 live）、242 本地采集器实弹三采集器全对（探针零残留）、243 MATURITY 证据刷新至 rounds 230–242、244 合并面 soak 回归全绿（RSS 127–146MB @3,312、双主题 smoke 0 错误）、245 竞品第十八批（claude-notify Telegram 远程回答落地——见上；kookr/kelpie 活跃、新进入者无新增）、246 分诊全流程复走全通 @3,312（✓ all done 35 项 + API 反 ack 归零）。
- Rounds 111–125 概要：111 detail 取全 P1（发 v0.4.6）、112/113 修复三面验证、114 文档入档、115 竞品复扫、116 键盘链全通、117 徽章对比度 P1（发 v0.4.7）、118 数据面干净、119 官网 doctor 页对比度 P1、120 交接整备、121 门禁/webhook 复测、122 竞品第五批、123 移动触控走查、124 本地采集器实弹、125 slim SSE P1（发 v0.4.8，daemon 0.4.0）。
- Rounds 223–234 概要（全部纯文档、无 P0/P1）：223 交接整备（补 213–222 收敛）、224 分诊全流程复走全通 @3,260、225 本地采集器实弹三采集器全对（Codex 生命周期注记：pending approval 只被 task_started/task_complete 清除，agent_message 不清除——按设计）、226 竞品第十六批（新进入者 claude-notify：presence-aware Telegram 推送+按钮回答，round-98 P2 观察项的野生实现，单 runtime 不重合聚合核心；claude-dispatcher Windows 落地）、227 移动端 Lighthouse 五跑 perf 91–94（中位 94）——round-214 中位 85 漂移不可复现，归因宿主机瞬态负载，观察项收口、228 数据面 3,277 全干净（连续第七个干净数据轮）、229 MATURITY 证据刷新至 rounds 218–228、230 PWA 离线+SSE 韧性复走全通（重启 ~5s 回 live 追平最快）、231 CLI 黄金路径全通（ls --waiting 5.4s@3,285、22 waiting，包络内）、232 合并面 soak 回归全绿（RSS 119–148MB @3,290）、233 分诊全流程复走全通（un-ack 契约坑见上）、234 竞品第十七批短窗（claude-dispatcher Cockpit v3：分诊变命令队列一次一 blocker——本地分诊人体工学跃升，保持提级盯防）。
- Rounds 213–222 概要（全部纯文档、无 P0/P1）：213 交接整备（补 200–212 收敛）、214 移动端 Lighthouse 五跑 perf 84–86（中位 85，较前四轮 94 下滑，paint 面非脚本面；TBT 79–160ms 远离触发线；零代码变更窗口无法归因，入档观察项待下个性能轮复测归因——总负责人已确认此安排）、215 数据面 3,246 全干净（waiting 8/8 detail+url、4.6–29.9 分钟全新鲜、连续第六个干净数据轮）、216 竞品第十五批（kookr 双静默窗口后恢复推送；claude-dispatcher 动作最大：v2 cockpit 八 lenses + worktree 隔离 + Windows 支持 + cockpit 默认化，仍 Claude-only；agentfleet 真实云机 provision 后续修复；新进入者扫描无新增）、217 MATURITY 证据刷新至 rounds 206–216（性能行诚实并入 round-214 观察项）、218 --host 门禁六面+webhook 复测（8 存量零误 POST、3 分钟 2 POST 全为合法新转换）、219 axe 复审 10 态 0 违规（与 147/162/178/196/206 持平）、220 CLI 黄金路径全通（ls --waiting 2.7s@3,254 追平最佳）、221 通知路径实测（8 分钟 5 次真实转换恰好 5 条 SW 通知、0 重复、✓ Done action 全对；坑见上：notify-ls 残留 off）、222 合并面 soak 回归全绿（15 分钟 @3,263，RSS 120–139MB 平稳零错误、双主题 smoke 0 错误）。
- Rounds 200–212 概要（全部纯文档、无 P0/P1）：200 交接整备（补 190–199 收敛）、201 移动端 Lighthouse 三跑 perf 全 94/TBT ≤10ms @3,214、202 --host 门禁六面+webhook 复测（16 存量零误 POST、3 分钟 5 POST 全为合法新转换 0 重复；坑：起测前清掉两个早期遗留 daemon）、203 合并面 soak 回归全绿（15 分钟 @3,226，RSS ~130MB 平稳零错误；坑见上：setsid 起探针）、204 数据面 3,229 全干净（waiting 15/15 detail+url、2.2–47.7 分钟全新鲜、连续第五个干净数据轮）、205 MATURITY 证据刷新至 rounds 196–204、206 axe 复审 10 态 0 违规（与 147/162/178/196 持平）、207 竞品第十四批（ccmux v1.3.0 移交正式化，其余七盯防静默、新进入者扫描无新增）、208 CLI 黄金路径全通（ls --waiting 3.6s@3,236）、209 PWA 离线+SSE 韧性复走全通（重启 ~5s 回 live 迄今最快）、210 合并面 soak 回归全绿（RSS 128–143MB @3,241）、211 分诊全流程复走全通（API 法反 ack 归零）、212 本地采集器实弹三采集器全对（探针零残留）。
- Rounds 190–199 概要（全部纯文档、无 P0/P1）：190 交接整备（补 180–189 收敛）、191 MATURITY 证据刷新至 rounds 187–189、192 合并面 soak 回归全绿（27 分钟 @3,186，RSS 爆爬暖机后回落 ~145MB 平稳、日志 0 错误）、193 CLI 黄金路径全通（ls --waiting 3.2s@3,191；hooks 三态用隔离 HOME 沙箱验证，含缺目录守卫）、194 数据面 3,191 全干净（waiting 30/30 迄今最大集、中位 15.5 分钟、连续第四个干净数据轮）、195 PWA 离线+SSE 韧性复走全通（杀 daemon 86 卡保留、冷开快照恢复、重启 ~10s 自动回 live）、196 axe 复审 10 态 0 违规（与 147/162/178 持平）、197 竞品短窗复核（agentfleet 创建当日即落真实云机端到端 provision——从 demo 走向实用，保持观察优先级；其余平静）、198 分诊全流程复走全通（方法注记：批量反 ack 探针不能盲打 j+e 循环——Needs You 列表随 un-ack 重排，应走 API 或逐卡点击）、199 本地采集器实弹三采集器全对（探针零残留）。
- Rounds 180–189 概要（全部纯文档、无 P0/P1）：180 合并面 soak 回归全绿（15 分钟 RSS 平稳零错误）、181 CLI 黄金路径全通（ls --waiting 2.7s@3,135，较 164 提速）、182 --host 门禁六面+webhook 复测（18 存量零误 POST、6 POST 全为合法转换；坑：webhook sink 起测前也要端口清场，旧 sink 残留会交叉日志无法归因，作废重跑）、183 MATURITY 证据刷新至 rounds 177–182、184 数据面 3,163 全干净（waiting 19/19、中位 8.9 分钟、连续第三个干净数据轮）、185 分诊全流程复走全通（附注：过滤器 localStorage 持久化会让上轮探针遗留 waiting 过滤影响首屏计数——按设计工作，探针起测先清 attnbox:filter）、186 竞品第十三批（新进入者三个全用 waiting/blocked-on-you 语言：agentfleet 远程多机控制面/waiting-on-me 七源 Claude 技能/streamdeck-agents 硬件外设；盯防五家全活跃）、187 通知路径实测（4 分钟 6 次真实转换恰好 6 条 SW 通知、0 重复）、188 本地采集器实弹三采集器全对、189 移动端 Lighthouse 三跑 perf 全 94/TBT 全 0ms @3,166。
- Rounds 171–178 概要（全部纯文档、无 P0/P1）：171 PWA 离线+SSE 韧性复走全通（杀 daemon 列表保留、断网冷开快照恢复 100 卡、重启 ~15s 自动回 live；坑：探针重启 daemon 要用真 daemon PID/脚本内 spawn，wrapper PID 会假失败）、172 移动端 Lighthouse 三跑 perf 全 94/TBT 0–59ms @3,130+（Lighthouse 要用 CHROME_PATH=Playwright chromium，本机 google-chrome 是 CDP shim 起不了）、173 MATURITY 证据刷新至 rounds 169–172、174 数据面 3,135 全干净（waiting 20/20、中位 16.5 分钟）、175 分诊全流程复走全通（方法注记：ack 状态在 /api/items 顶层 acked map 而非 item 字段）、176 竞品第十二批（kelpie VISION 注意力语言同源、claude-dispatcher 入档）、177 本地采集器实弹三采集器全对（探针零残留）、178 axe 复审 10 态 0 违规。
- Rounds 161–169 概要（全部纯文档、无 P0/P1）：161 本地采集器实弹首次单轮全覆盖三采集器（Claude tool_use→waiting→working、Codex 审批→waiting/approve 带命令预览→idle、Gemini 只报 working/idle 诚实边界成立）、162 axe 复审 10 态 0 违规（与 147 持平）、163 --host 门禁六面+webhook 存量零误 POST（round-150 端口清场坑再次应用）、164 CLI 黄金路径全通（ls --waiting 3.8–5.6s@3,110+）、165 MATURITY 证据刷新至 rounds 156–164、166 分诊全流程复走全通+round-154 搜索空结果观察项 5 连跑不可复现（维持不定级）、167 数据面 3,127 全干净（waiting 20/20 detail+url、中位 14 分钟）、168 竞品第十一批（四盯防全活跃、连续第二个安静窗口）、169 合并面 soak 回归全绿（RSS 平稳 @3,133）。
- Rounds 151–159 概要（全部纯文档、无 P0/P1）：151 CLI 黄金路径复走全通、152 移动端 Lighthouse perf 中位 94/TBT ≤10ms（round-134 后首测，slim 增益略升）、153 竞品第九批（agent-deck 404、kelpie 依附 herdr 回归）、154 分诊全流程复走全通（附观察项：重度 churn 窗口搜索可短暂空结果，复跑稳定）、155 MATURITY 刷新至 round-155 证据、156 数据面 3,094 全干净（waiting 25/25 detail+url）、157 通知路径实测（实机两次转换恰好各一条 SW 通知+✓ Done，风暴防护成立）、158 合并面 soak 回归全绿（RSS 平稳 @3,105）、159 竞品第十批（kookr/kelpie 双活跃，新进入者扫描首次为空）。
- Rounds 141–149 概要（全部纯文档、无 P0/P1）：141 --host 门禁+webhook 零误 POST 复测、142 本地采集器实弹（Claude 启发式三态全对）、143 MATURITY 实证行刷新、144 竞品第八批（herdr 25.5k★ 生态出现 waiting-on-you 插件——runtime 自带注意力面成趋势；nMn 自动批准反方案；claude-notify presence-aware 再验证）、145 slim 分诊面 UX 走查全通、146 合并面 soak 回归全绿（RSS 平稳 @3,024）、147 axe 复审 10 态 0 违规（附 done 态标准审计法）、148 数据面 3,069 全干净、149 PWA 离线+SSE 韧性复走全通。
- Rounds 127–139 概要（全部纯文档、无 P0/P1）：127 官网 slim 边界入档+竞品必查（官网已重建核验）、128 数据面 2,990 全干净、129 slim SSE 端到端实机走查全通、130 axe 复审 10 态 0 违规、131 竞品第六批（agent-deck 入档、kelpie 404 确认）、132 CLI 黄金路径复走全通、133 合并面 soak 回归全绿、134 发布后 Lighthouse perf 中位 92（round-125 增益保持）、135 分诊全流程复走全通、136 MATURITY 刷新至 v0.4.8、137 reply act-in-place 复测全通（受控探针）、138 竞品第七批（ccmux handoff 示警）、139 数据面 3,009 全干净。对外可见性/宣发仍是最大非工程缺口（待老板决策）。
